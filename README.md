# TW site-reporter front-end

Front-end for a tool to run Lighthouse and accessibility reports on entire sites.

In the project directory, run `yarn start` to run the app in the development mode, or `yarn build` to generate a deploy-optimized build.